<%= render 'details_form', holiday: @holiday %>

<%= link_to t('edit'), edit_holiday_path(@holiday) %> |
<%= link_to t('back'), holidays_path %> |
<%= link_to 'PDF', holiday_path(@holiday, format: "pdf") %>
